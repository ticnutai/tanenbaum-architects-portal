# סיידבר הקלטה חי ב־BrowserOS

## מה נבנה

התוסף `מקליט מבא״ת` מציג בצד הדפדפן את השלבים שנקלטים בזמן אמת. הוא מיועד לתצוגה
ולניהול נוח בלבד; הוא אינו מנוע אוטומציה נוסף.

- Python ו־Raw CDP הם מקור האמת היחיד להקלטה.
- Playwright הוא מנוע ההרצה היחיד של התהליך השמור.
- Electron הוא העורך המלא של אוטומציות, נתונים, סיסמאות ודוחות.
- תוסף BrowserOS הוא שלט קטן ותצוגת שלבים חיה.
- חיבור ה־MCP שמופיע ב־BrowserOS תחת Connected agents הוא ערוץ השליטה והאבחון של
  התוכנה והסוכנים. התוכנה קוראת את הפורטים הדינמיים של BrowserOS ומעדיפה את כתובת
  ה־proxy שמוצגת שם (כעת `http://127.0.0.1:9001/mcp`), עם תמיכה גם בפורט השרת של
  גרסאות ישנות.

אין בפרויקט `iframe` של אתר מבא״ת, אין שימוש בהרשאת `debugger`, אין
`nativeMessaging`, ואין הרשאת `<all_urls>`.

## התקנה מקומית

1. בנה את התוסף באמצעות `npm run extension:build`.
2. פתח באפליקציית Electron את **הגדרות ← סיידבר הקלטה ב־BrowserOS**.
3. לחץ **פתיחת תיקיית התוסף**.
4. ב־BrowserOS פתח את מסך התוספים, הפעל מצב מפתח ובחר **טעינת תוסף שלא נארז**.
5. בחר את התיקייה `browser-extension/.output/chrome-mv3`.
6. חזור להגדרות, לחץ **יצירת קוד חיבור**, פתח את סמל התוסף והזן את הקוד בן שש הספרות.

לאחר החיבור די בלחיצה על סמל התוסף כדי לפתוח את הסיידבר. פתיחה אוטומטית ללא
לחיצת משתמש אינה נתמכת על ידי מנגנון ה־Side Panel של Chromium.

## הפעולות הזמינות

- התחלת הקלטה ועצירתה.
- ביטול השלב האחרון, גם באמצעות `Ctrl+Z` כשהסיידבר בפוקוס.
- שינוי שם, שכפול, השהיה, הפעלה ומחיקה של שלב.
- הצגת צילום של שלב לפי דרישה בלבד.
- פתיחת עורך ההקלטה המלא באפליקציית Electron.
- חיבור מחדש אוטומטי לאחר ניתוק קצר וקבלת תמונת מצב מלאה לאחר החיבור.

ערכי סיסמה לעולם אינם מועברים לתוסף. הוא מקבל רק מצב כללי כגון "סיסמה שמורה
בכספת". קוד החיבור תקף לעשר דקות, אסימון החיבור נשמר רק באחסון המקומי של התוסף,
ובקובץ ההגדרות של התוכנה נשמר רק הגיבוב שלו.

## בדיקות

- `python -m unittest discover -s tests -v`
- `python tools/test_architecture.py`
- `npm run extension:build`
- `npm run test:extension-sidepanel` כאשר שרת Python מבודד פועל בפורט 18473.

בדיקת הסיידבר טוענת את התוסף ב־Chromium headless, מבצעת חיבור אמיתי בקוד חד־פעמי,
בודקת WebSocket חי, עריכה, שכפול, ביטול, השהיה והפעלה, ואוספת שגיאות Console.
