# סיכום מצב מערכת האוטומציה למבא״ת

**תאריך עדכון:** 11 באוגוסט 2026
**מיקום הפרויקט:** `C:\Users\jj121\Documents\ChatGPT\מבאת`

## תקציר מנהלים

המערכת מסודרת כעת סביב ארכיטקטורה פעילה אחת: יישום Electron עם ממשק React בעברית, שירות Python מקומי, וחלון Google Chrome חיצוני ייעודי המחובר באמצעות CDP. אין שימוש ב־iframe, ב־Electron `webview` או בדפדפן Chromium מוטמע לצורך העבודה באתר מבא״ת.

תשתית המערכת עובדת: האפליקציה עולה, Chrome מתחבר, התצוגה החיה פועלת, ניתן להקליט ולנהל שלבים, להפעיל שלבים, לנהל סיסמאות באופן מאובטח ולפתוח קונסול עם רענון והעתקה. הבנייה והבדיקות הקיימות עוברות.

עם זאת, חשוב לדייק: **האוטומציה העסקית המלאה של יצירת תוכנית במבא״ת עדיין אינה מוכנה להפעלה עצמאית בייצור מתחילתה ועד סופה.** חלק מהשלבים הקיימים עדיין ידניים או מכילים `{TODO}`, ויש להשלים מיפוי ואימות חי של הטפסים וה־Dropdowns בתוך אתר מבא״ת.

## הארכיטקטורה שנבחרה

```text
Electron
  └─ ממשק React בעברית וב־RTL — פורט 18474
       ├─ ניהול אוטומציות
       ├─ הקלטת פעולות
       ├─ עריכת שלבים
       ├─ הפעלה ויומן
       └─ קונסול מערכת

Python API — פורט 18473
  ├─ שמירת תהליכים ונתונים
  ├─ ניהול סיסמאות
  ├─ הקלטה ותצוגה
  ├─ יומן ודוחות
  └─ חיבור ל־Chrome

Google Chrome חיצוני רגיל בלבד
  ├─ קובץ הפעלה: chrome.exe
  ├─ פרופיל ייעודי: .runtime/chrome/mavat
  ├─ CDP: פורט 9223
  └─ חשבון העבודה שהוגדר: jj1212t@gmail.com

Playwright
  ├─ מתחבר ל־Chrome הקיים דרך CDP
  ├─ מבצע אוטומציות ובדיקות
  └─ אינו פותח Chromium מוטמע לצורך העבודה הרגילה
```

## ההחלטה לגבי iframe, Chromium ו־Chrome

בתחילת הפיתוח נבדקה אפשרות להציג ולשלוט באתר בתוך iframe או דפדפן מוטמע. האפשרות ננטשה משום שאתרי ממשלה, התחברות, iframes חוצי־דומיינים ומנגנוני אבטחה אינם עובדים בה באופן יציב.

ההחלטה הסופית:

- עובדים עם חלון Google Chrome חיצוני ורגיל.
- Chrome מוצמד לצד Electron כאשר רוצים סביבת עבודה משולבת.
- Electron מציג צילום חי ואמצעי שליטה, אך אינו מכיל דפדפן שני.
- Playwright מתחבר לחלון Chrome הקיים באמצעות CDP.
- המילה `playwright.chromium` בקוד היא שם ממשק התכנות של Playwright למשפחת הדפדפנים; הדפדפן שנפתח בפועל הוא Google Chrome באמצעות `channel="chrome"` או חיבור CDP.

## רכיבי המערכת הפעילים

### 1. ממשק Electron ו־React

- ממשק עברי ו־RTL.
- סרגל ניווט קבוע.
- כפתור חזרה למסך הקודם.
- תמיכה בהפעלה כיישום Electron ובמצב פיתוח מקומי.
- נעילת מופע יחיד: הפעלה נוספת מחזירה את החלון הקיים ואינה פותחת Electron נוסף.

### 2. אוטומציות

- ניתן ליצור כמה אוטומציות נפרדות.
- לכל אוטומציה רשימת שלבים ושדות קלט משלה.
- ניתן להרכיב אוטומציה חדשה משלבים של אוטומציות קיימות.
- ניתן לבחור שלב יחיד או כמה שלבים להרצה.

### 3. הקלטת פעולות

- מסך הקלטה נפרד בנתיב `/recorder`.
- קליטת לחיצות, מילוי שדות ובחירות Dropdown מתוך Chrome המחובר.
- בניית כמה אסטרטגיות זיהוי לרכיב: תווית, role, placeholder, test id, CSS, טקסט ומיקום כגיבוי.
- צילום קטן לכל שלב רגיל כאשר התצוגה זמינה.
- ערך של שדה סיסמה אינו נשמר ואינו מצולם.
- מנגנון מניעת כפילות מתעלם מקליטה חוזרת זהה בתוך שלוש שניות.

### 4. עריכת שלבים

- הוספה, עריכה והכנסה בין שלבים.
- בחירה מרובה.
- העתקה, השהיה, הפעלה ומחיקה קבוצתית.
- שינוי סדר השלבים.
- הרצת שלבים נבחרים בלבד.
- קישור שלב סיסמה לפרופיל כניסה מאובטח.

### 5. תצוגת Chrome

- תצוגה חיה בתוך האפליקציה באמצעות צילומים מ־CDP.
- בחירת לשונית מבא״ת פעילה.
- אחורה, קדימה, רענון וגלילה.
- מצב סימון רכיב ומצב לימוד.
- אפשרות להעביר את חלון Chrome לחזית.
- אפשרות להצמיד את Chrome ואת Electron זה לצד זה.
- אפשרות לקשר מזעור ושחזור של שני החלונות.

### 6. סיסמאות וכניסה

- סיסמאות אינן נשמרות בקוד, ב־Excel או בקובץ תהליך.
- שמירה מתמשכת מתבצעת באמצעות Windows Credential Manager.
- ניתן להחליף או למחוק סיסמה מתוך עורך הסיסמאות.
- OTP, אימות ביומטרי, CAPTCHA ואישורים ממשלתיים נשארים שלבים ידניים.

### 7. קונסול, יומן ודוחות

- בפינה התחתונה קיים אייקון קונסול קטן.
- לחיצה פותחת קונסול המציג את מצב React, Python, Chrome CDP, דפים פעילים, יומן Python ואירועי Console של Chrome.
- בקונסול קיימים כפתורי רענון, העתקה וסגירה.
- אייקון רענון קטן זמין גם כשהקונסול סגור.
- מסך יומן הריצה משתמש באותו רכיב קונסול; אין מימוש כפול.
- ניתן להעתיק דוח שגיאות ולייצא את היומן ל־CSV.

## מצב חי בעת כתיבת המסמך

הבדיקה בוצעה מול המערכת הפועלת בפועל:

| רכיב | כמות | מצב |
|---|---:|---|
| חלון Google Chrome ייעודי | 1 | מחובר |
| מופע Electron ראשי | 1 | פעיל |
| שרת React | 1 | מאזין בפורט 18474 |
| שירות Python API | 1 | מאזין בפורט 18473 |
| חיבור CDP | 1 | מאזין בפורט 9223 |
| תהליך תצוגה חיה | 1 | פעיל |

Chrome היה מחובר לדף:

`https://www.gov.il/he/service/mvat`

גרסת הדפדפן שנקלטה בבדיקה: `Chrome/151.0.7922.76`.

## מניעת כפילויות ובלבול

בוצעו הפעולות הבאות:

- Electron מוגבל למופע ראשי אחד.
- יש פרופיל Chrome ייעודי אחד ופורט CDP אחד.
- תהליך התצוגה סוגר גם את תהליכי Playwright הבנים שלו כדי שלא יישארו תהליכים יתומים.
- מנגנון ההקלטה מסנן אירועים זהים שנקלטו פעמיים ברצף.
- דפי HTML הישנים של Python אינם משמשים עוד כממשק מקביל; כתובותיהם מפנות לממשק React.
- API שפתח את הממשק הקלאסי הוסר מהמסלול הפעיל.
- קונסול המערכת אוחד לרכיב גלובלי אחד.
- בדיקת ארכיטקטורה אוטומטית מונעת החזרה של iframe, `webview`, פורט 9222 או Chromium כדפדפן העבודה.

קבצי הממשק הקלאסי עדיין קיימים בתיקייה לצורכי תאימות ובדיקות ישנות בלבד, אך `run_app.bat` אינו מפעיל אותם.

## בדיקות שעברו

- `npm run build` — בניית React ושרת ה־SSR עברה.
- `npx tsc --noEmit` — בדיקת TypeScript עברה.
- בדיקות API של ממשק Web עברו.
- בדיקות ניהול סיסמאות עברו.
- בדיקת בחירה מרובה ועריכת שלבים עברה.
- בדיקת מנוע Playwright עם Dropdown עברה.
- בדיקת הארכיטקטורה עברה.
- בדיקה חזותית של `/recorder` עברה.
- פתיחת הקונסול, רענונו והעתקת תוכנו נבדקו בפועל.
- מספר המופעים והתהליכים נבדק בזמן אמת ונמצא תקין.

## מה עדיין לא הושלם

### נדרש לפני שימוש ייצור מלא

1. לעבור יחד על כל מסכי מבא״ת ולהקליט תהליך מלא מתחילתו ועד סופו.
2. למפות את כל שדות הטופס למשתני הקלט, למשל ועדה, סוג תוכנית, גוש, חלקה, מגרש ושטח.
3. להחליף שלבים שמכילים `{TODO}` בערכים או משתנים אמיתיים.
4. להחליף שלבי Dropdown ידניים בפעולות `select_option` או באסטרטגיית בחירה חכמה מאומתת.
5. לבדוק את תהליך הכניסה, העצירה הידנית והחזרה להרצה לאחר אימות ממשלתי.
6. לבצע הרצת E2E חיה על רשומת בדיקה שאינה מסכנת מידע אמיתי.
7. לנקות ידנית שלבים היסטוריים כפולים ברשימת 25 השלבים רק לאחר שנאשר אילו מהם מיותרים; המערכת אינה מוחקת שלבי משתמש אוטומטית.

### מנגנון הרקע הפעיל

ההפרדה הטכנית הושלמה:

- המקליט, התצוגה החיה וקונסול הדפדפן משתמשים בצופה Raw CDP ישיר ברקע.
- אירועי הקלטה, מצב וקונסול נשלחים ל־React בזמן אמת דרך WebSocket בנתיב `/ws/events`.
- Playwright נשאר מנוע ביצוע בלבד: הרצת אוטומציה, פעולה יזומה ובדיקת E2E.
- הממשק מציג חיווי מפורש `WebSocket מחובר` ו־`קולט Raw CDP ברקע`.
- רענון מחזורי איטי נשאר רק כגיבוי למקרה שחיבור WebSocket נקטע זמנית.

הצופה קולט פעולות בתוך דפי האתר לאחר הפעלת ההקלטה. הוא אינו מקליט את ממשק Chrome עצמו, חלונות מערכת, CAPTCHA או מנהל סיסמאות של הדפדפן.

## כיצד מפעילים נכון

ההפעלה הרשמית היחידה למשתמש היא:

```bat
run_app.bat
```

הקובץ מעלה את Electron, React ו־Python. מתוך האוטומציה פותחים את דפדפן העבודה הייעודי. אין להפעיל במקביל את `run_classic.bat`, ואין צורך לפתוח שרת Python נוסף באופן ידני.

## מסקנה

המערכת מסודרת כעת ברמת התשתית והפיתוח: קיימת ארכיטקטורה אחת, החיבורים והתהליכים אינם כפולים, הממשק והקונסול עובדים והבדיקות הקיימות עוברות.

המערכת עדיין נמצאת בשלב **פיתוח ואימות האוטומציה העסקית**, ולא בשלב שבו ניתן להבטיח שכל תוכנית תוזן אוטומטית למבא״ת ללא ליווי. השלב הבא הוא להשלים ולהוכיח תהליך מבא״ת מלא על נתוני בדיקה, שלב אחר שלב.

## מסמכי מקור אמת

- `ARCHITECTURE.md` — כללי הארכיטקטורה שאסור לשבור.
- `CURRENT_SYSTEM_STATUS_HE.md` — מסמך המצב הנוכחי.
- `README_HE.md` — הוראות התקנה והפעלה.
- `tools/test_architecture.py` — בדיקת רגרסיה של הארכיטקטורה.
