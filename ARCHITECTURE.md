# ארכיטקטורת מערכת מבא״ת — מקור אמת

המסמך הזה מגדיר את הארכיטקטורה הפעילה. שינוי שסותר אותו נחשב רגרסיה.

## רכיבים פעילים

1. Electron מציג את ממשק React המקומי ב־`http://127.0.0.1:18474`.
2. Python מספק API מקומי בלבד ב־`http://127.0.0.1:18473`.
3. הדפדפן הוא **Google Chrome חיצוני רגיל בלבד** (`chrome.exe`).
4. Chrome משתמש בפרופיל הייעודי היחיד `.runtime/chrome/mavat` ובפורט CDP ‏`9223`.
5. צופה Python פסיבי מתחבר ישירות ל־Raw CDP WebSocket ומקליט אירועי DOM וקונסול ברקע.
6. אירועי הצופה מועברים ל־React בזמן אמת דרך `/ws/events`; polling משמש גיבוי איטי בלבד.
7. Playwright מתחבר ל־Chrome הקיים דרך CDP ומבצע פעולות רק בזמן הרצה, פעולה יזומה או בדיקה.
8. התצוגה בתוך React היא צילום חי דרך Raw CDP; היא אינה דפדפן נוסף.

## אסור להחזיר

- `iframe` או Electron `webview` לצורך הטמעת אתר מבא״ת.
- Chromium שמגיע עם Playwright כדפדפן העבודה.
- פרופיל אוטומציה שני או פורט CDP נוסף.
- ממשק HTML/Python מקביל לממשק React הפעיל.
- יותר ממופע Electron ראשי אחד.

המזהה `playwright.chromium` הוא שם ה־API של Playwright לתקשורת עם דפדפנים ממשפחת Chromium. הוא אינו מאשר הפעלה של Chromium המובנה: פתיחה חדשה חייבת להשתמש ב־`channel="chrome"`, והנתיב הראשי מתחבר ל־Google Chrome דרך CDP.
